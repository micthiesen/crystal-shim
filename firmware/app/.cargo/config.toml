[target.riscv32imac-unknown-none-elf]
rustflags = ["-C", "link-arg=-Tlinkall.x"]

[build]
target = "riscv32imac-unknown-none-elf"

# No flash runner: hardware bindings are uncommissioned and flashing requires authorization.
